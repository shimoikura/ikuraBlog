<h1>ikura Blog</h1>
<p><%= link_to '戻る', blogs_path %></p>
<div class="blog-detail">
  <%= form_for( [@blog, @blog.replies.build] ) do |f| %>
  <p>
    <strong><%= @blog.body %></strong>

  </p>
  <p>
    <%= f.label :comment %><br />
    <%= f.text_area :comment, cols: 60, rows: 4 %>
  </p>

  <p>
    <%= f.submit %>
  </p>
  <hr>
  <% @blog.replies.each do |reply| %>
    <p>
      <span>コメント：</span>
      <strong><%= reply.comment %></strong>

    </p>
  <% end %>
</div>

<% end %>
